<%= react_component("Navigation", {}) %>
<%= react_component("AccountEdit", {
  firstName: current_player.first_name,
  lastName: current_player.last_name,
  foot: current_player.foot,
  highschool: current_player.highschool,
  age: current_player.age,
  graduationYear: current_player.graduation_year,
  birthday: current_player.birthday,
  highschoolGoals: current_player.highschool_goals,
  highschoolAssists: current_player.highschool_assists,
  highschoolPosition: current_player.highschool_position,
  state: current_player.state,
  city: current_player.city,
  streetAddress: current_player.street_address,
  phoneNumber: current_player.phone_number,
  clubName: current_player.club_name,
  clubGoals: current_player.club_goals,
  clubAssists: current_player.club_assists,
  clubPosition: current_player.club_position,
  height: current_player.height,
  weight: current_player.weight,
  highlightVideoLink: current_player.highlight_video_link,
  fullGameLink: current_player.full_game_link,
  satScore: current_player.sat_score,
  actScore: current_player.act_score,
}) %>
